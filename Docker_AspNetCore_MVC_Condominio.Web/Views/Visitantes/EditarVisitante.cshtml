@model Docker_AspNetCore_MVC_Condominio.Web.Models.ImovelViewModel
@{
    ViewData["Title"] = "Editar Visitante";
}

<div class="card shadow-sm border-0 rounded-3 p-4 bg-white">
    <div class="d-flex align-items-center gap-2 mb-4 pb-2 border-bottom">
        <i class="bi bi-person-vcard text-primary fs-3"></i>
        <h4 class="fw-bold text-secondary m-0">@ViewData["Title"]</h4>
    </div>

    <form asp-controller="Visitantes" asp-action="EditarVisitante" autocomplete="off">
        <input type="hidden" asp-for="Visitante!.Id" />
        <input type="hidden" asp-for="Visitante!.ImovelId" />
        <input type="hidden" asp-for="Visitante!.EmpresaId" />
        <input type="hidden" asp-for="Imovel!.Id" />
        <input type="hidden" asp-for="Imovel!.Apartamento" />
        <input type="hidden" asp-for="Imovel!.Bloco" />

        <partial name="_ImovelBlocoApto" />

        <div class="row g-4">
            <div class="col-12">
                <h6 class="fw-bold text-primary mb-3"><i class="bi bi-card-heading me-1"></i> Dados de Identificação</h6>
                <div class="row g-3">

                    <div class="col-md-7">
                        <div class="form-floating">
                            <input asp-for="Visitante!.Nome" class="form-control" placeholder="Nome Completo" />
                            <label asp-for="Visitante!.Nome">Nome Completo do Visitante</label>
                            <span asp-validation-for="Visitante!.Nome" class="text-danger small"></span>
                        </div>
                    </div>
                    <div class="col-md-2">
                        <div class="form-floating">
                            <input asp-for="Visitante!.DataEntrada" asp-format="{0:yyyy-MM-dd}" class="form-control" />
                            <label asp-for="Visitante!.DataEntrada">Data Entrada</label>
                        </div>
                    </div>
                    <div class="col-md-2">
                        <div class="form-floating">
                            <input type="date" asp-for="Visitante.DataSaida" asp-format="{0:yyyy-MM-dd}" class="form-control" />
                            <label asp-for="Visitante.DataSaida">Data de Saída</label>
                            <span asp-validation-for="Visitante.DataSaida" class="text-danger small"></span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="d-flex gap-2 mt-4 pt-3 border-top">
            <button type="submit" class="btn btn-success px-4 shadow-sm fw-medium">
                <i class="bi bi-floppy"></i> Salvar
            </button>
            <a asp-action="ListarVisitantes" asp-route-id="@Model.Visitante!.Id" class="btn btn-secondary px-4 fw-medium">
                <i class="bi bi-x-circle"></i> Cancelar
            </a>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}